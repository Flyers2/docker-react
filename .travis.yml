sudo: required


services:
  - docker

before_install:
  - docker build -t flyters33/docker-react -f Dockerfile.dev .

script:
   -  docker run -e CI=true flyters633/docker-react npm run test -- --covarage